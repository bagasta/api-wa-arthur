# LangChain API Endpoint
LANGCHAIN_API_URL=https://backend-ai.chiefaiofficer.id

# Connects directly to the DB without going through the main backend
DATABASE_URL=postgresql://bagas:bagas123!@localhost:5432/langchain_api

# N8N Webhook that triggers on incoming WhatsApp message
# (You will need to create this Webhook node in n8n)
N8N_WEBHOOK_URL=https://b2hqp564-8100.asse.devtunnels.ms/webhook/whatsapp-trigger
